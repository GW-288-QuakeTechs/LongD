// Import necessary dependencies
import { Component, OnInit } from '@angular/core';

@Component({
  // Component metadata
  selector: 'app-login', // CSS selector for embedding the component
  templateUrl: './login.page.html', // HTML template associated with the component
  styleUrls: ['./login.page.scss'], // External style files for component styling
})
export class LoginPage implements OnInit {
  constructor() { }

  ngOnInit() {
    // Initialize component and perform any required setup
  }
}
